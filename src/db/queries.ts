// import { db, pgp } from './postgres';
// import type pgPromise from 'pg-promise';

// const productsColumnSet = new pgp.helpers.ColumnSet(['name', 'barcode'], {
//   table: 'products',
// });

// // const query = {
// //   many: async (query: pgPromise.QueryParam) => {
// //     return db.query(query);
// //   },
// //   insert: async (query: pgPromise.QueryParam) => {
// //     return db.any(query);
// //   },
// // };
